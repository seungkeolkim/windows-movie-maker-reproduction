# 인터랙티브 목업 검증 시나리오

- 문서 번호: `MOCK-0002`
- 상태: 승인됨
- 단계: 기능 정의 및 UI 목업 작업 방식의 5단계
- 작성일: 2026-09-06
- 동작 기준: [MOCK-0001: 목업 동작 및 상태 명세](mock-0001-behavior-specification.md)
- 화면 기준: [DESIGN-0001](../design/design-0001-screen-layout.md),
  [DESIGN-0002](../design/design-0002-element-functions.md)

## 목적

이 문서는 W-02 실제 미디어 보관함, W-03 실제 프로젝트 파일, W-04 실제 타임라인 편집,
W-05 실제 시각 미리 보기, W-06 실제 오디오 미리 듣기와 고정 샘플 데이터를 함께 사용해
MVP·1.0의 대표 흐름, 빈 상태,
오류 상태와 반응형 레이아웃을 같은 절차로 다시 검증하기 위한 기준이다. 통과 결과는
[MOCK-0003](mock-0003-review-log.md)에 기록한다.

목업 통과는 MP4 출력이나 내레이션·파형·페이드가 구현됐다는 뜻이 아니다. 프로젝트 직렬화는
W-03, MVP 타임라인 편집은 W-04, 시각 프레임은 W-05, 원본음·음악 믹싱은 W-06에서 실제
서비스로 교체됐다.
현재 검증 대상은 기능 발견 가능성, 화면 배치, 입력에 따른 상태 변화, 피드백과 실제 서비스로
교체할 경계다.

## 실행 방법

개발 환경을 준비한 뒤 저장소 루트에서 다음 명령으로 목업을 실행한다.

```powershell
uv --managed-python run --locked --no-sync -- movie-maker
```

메뉴의 `목업 상태`에서 빈 프로젝트, 샘플 편집 프로젝트, 누락 미디어, 부분 가져오기 실패,
출력 실패, 자동 저장 복구, 온보딩과 런타임 상태를 직접 재현할 수 있다. `목업`, `목업 값`
표시는 출력과 1.0 오디오 기능 등 후속 기능을 실제 처리 결과로 오해하지 않게 계속 노출한다.

자동 검증과 기준 화면 캡처는 다음 명령을 사용한다.

```powershell
uv --managed-python run --locked --no-sync -- pytest
uv --managed-python run --locked --no-sync -- python scripts/mock/capture_mock.py
```

## 구현 경계와 파일 대응

| 책임 | 현재 목업 구현 | 실제 배선 단계의 교체 대상 |
| --- | --- | --- |
| 화면과 사용자 의도 전달 | `src/movie_maker/ui/main_window.py` | 유지하며 서비스 호출 어댑터만 교체 |
| 보조 화면과 대화상자 | `src/movie_maker/ui/dialogs.py` | 프로젝트 파일 선택, 장치 및 작업 서비스 연결 |
| 고정 샘플 상태 구조 | `src/movie_maker/ui/mock_model.py` | 프로젝트·미디어 도메인 모델 |
| 실제 보관함·프로젝트 파일·타임라인·재생 시간·오디오 속성과 가짜 후속 상태의 경계 | `src/movie_maker/ui/mock_controller.py` | 출력 및 1.0 서비스 |
| 실제 source 프레임 계산·디코딩·최신 요청 취소 | `src/movie_maker/preview/`, `src/movie_maker/ui/preview.py` | W-07 출력에서 시간 매핑 재사용 |
| 실제 원본음·음악 그래프, PCM 디코딩·장치와 최신 요청 취소 | `src/movie_maker/audio/`, `src/movie_maker/ui/audio.py` | W-07 출력에서 공통 그래프 재사용 |
| UI 및 상태 회귀 검증 | `tests/ui/` | 실제 서비스 계약 테스트와 함께 유지·확장 |

위젯은 목업 상태를 직접 수정하지 않는다. 사용자 입력은 `MockController`의 의도 메서드로
전달되고, 컨트롤러가 상태를 바꾼 뒤 신호로 화면을 다시 그리게 한다. W-04 타임라인 행동은
[ADR-0002](../decisions/adr-0002-command-based-edit-history.md)의 명령별 최소 역연산 계약을
사용한다. 전체 상태 복사는 다중 선택·복제·효과처럼 남은 목업 행동에만 사용한다.

## 공통 사전 조건

- 애플리케이션을 새로 실행하면 `제목 없음`, 빈 보관함과 빈 타임라인에서 시작한다.
- `미디어 가져오기`는 운영체제 다중 파일 선택 창에서 선택한 실제 파일을 분석한다.
- 후속 목업 흐름은 `샘플 편집 프로젝트 불러오기`의 정상 샘플 다섯 개를 사용한다.
- `프로젝트 열기`와 `샘플 편집 프로젝트 불러오기`는 19초짜리 `제주 여행 목업`을 연다.
- 선택한 원본은 읽기만 한다. 프로젝트 파일은 실제로 만들며 MP4 파일은 아직 만들지 않는다.
- 정상 흐름과 오류 흐름 모두 현재 프로젝트의 편집 상태를 잃지 않아야 한다.

## MVP 핵심 흐름

### V-MVP-01: 빈 상태에서 미디어 가져오기

연결 기능: `F-PROJECT-01`, `F-MEDIA-01`~`03`, `05`, `F-EXPERIENCE-01`~`04`.

1. 새 목업을 실행하거나 `빈 프로젝트로 초기화`한다.
2. 시작 화면, 보관함, 타임라인과 비활성화된 출력 명령을 확인한다.
3. 시작 화면의 `미디어 가져오기`를 누른다.
4. 지원되는 영상·사진·오디오를 운영체제 창에서 함께 선택한다.
5. 전체·영상·사진·오디오 필터를 차례로 선택한다.

합격 기준:

- 시작 화면에서 가져오기가 가장 강조되고 새 프로젝트와 열기는 보조 행동이다.
- 성공 항목이 종류, 이름, 길이 또는 사진 표시, 상태와 썸네일로 구분된다.
- 필터는 실제 보관함과 숨겨진 선택을 변경하지 않는다.
- 원본 파일의 내용, 크기와 수정 시간은 바뀌지 않는다.
- 가져오기 결과, 수정됨 상태와 다음 행동이 즉시 표시되며 취소는 상태를 바꾸지 않는다.

자동 검증: `test_default_selector_uses_native_multi_file_dialog`,
`test_main_window_import_action_uses_selected_files_and_shows_failures`,
`test_library_filter_keeps_selection_state_but_changes_visible_items`.

### V-MVP-02: 고정 트랙 배치와 원본 화면 결정

연결 기능: `F-TIMELINE-01`, `F-AUDIO-03`, `04`, `F-VISUAL-04`.

1. `해변 산책.mp4`, `한라산 사진.jpg`, `여행 배경음악.mp3`를 차례로 추가한다.
2. `장소 설명.wav`를 내레이션 용도로 추가한다.
3. 프로젝트 속성의 화면 요약과 네 고정 트랙을 확인한다.
4. 음악 클립의 시작, 사용 구간, 음량과 음소거 값을 바꾸고 적용한다.

합격 기준:

- 영상·사진은 하나의 순차 스토리 라인, 음악과 내레이션은 각 고정 트랙에 놓인다.
- 첫 영상인 `해변 산책.mp4`가 원본 유지의 기준이 되고 1920×1080이 고정된다.
- 다른 비율의 세로 영상과 사진은 기본 `맞춤`과 중립 여백 규칙을 설명한다.
- 음악 속성 변경은 고정 음악 트랙과 출력 요약의 프로젝트 길이를 일관되게 유지한다.
- 미디어 선택과 클립 선택은 서로 해제되고 속성 문맥이 즉시 바뀐다.

자동 검증: `test_controller_adds_real_clips_to_all_fixed_media_tracks`,
`test_timeline_selection_updates_preview_and_inspector`.

### V-MVP-03: 순서, 트리밍, 사진 길이와 영상 속도

연결 기능: `F-TIMELINE-02`, `05`~`07`, `F-AUDIO-01`, `02`.

1. 샘플 프로젝트에서 `야시장` 클립을 선택한다.
2. 원본 시작·끝을 0.5초 단위로 바꾸고 적용한다.
3. 속도를 2×, 원본음 65%, 음소거로 바꾸고 적용한다.
4. 사진을 선택하여 표시 시간을 1~30초 안에서 바꾼다.
5. 시각 클립을 앞이나 뒤로 이동한다.

합격 기준:

- 영상 결과 길이는 선택한 원본 구간을 속도로 나눈 값이다.
- 잘못된 구간, 0.5초 미만 영상·오디오와 1~30초 밖의 사진 길이는 적용되지 않는다.
- 순서와 길이 변화가 타임라인, 전체 길이, 재생 위치와 미리 보기에 함께 반영된다.
- 한 번의 `속성 적용`은 행동 이력 하나로 기록된다.

자동 검증: `test_property_apply_is_one_real_command_and_save_keeps_history`와
`tests/timeline/test_editing.py`의 트리밍·사진 길이·유리수 속도 검증.

### V-MVP-04: 실수 구간 분할과 삭제

연결 기능: `F-TIMELINE-03`, `04`, `F-PREVIEW-03`, `04`.

1. `해변 산책`을 선택하고 재생 헤드를 클립 안쪽 4초에 둔다.
2. `분할` 또는 `Ctrl+B`를 실행한다.
3. 경계 0.25초 이내에서도 분할을 시도해 제한 안내를 확인한다.
4. 가운데 결과를 선택해 삭제한다.

합격 기준:

- 유효한 분할은 같은 원본을 참조하는 두 클립을 만들고 뒤쪽 결과를 선택한다.
- 클립 경계와 사진에서는 조용히 실패하지 않고 이유를 표시한다.
- 시각 클립 삭제 뒤 빈 시간이 사라지고 전체 길이와 재생 헤드가 유효 범위로 조정된다.
- 보관함 항목과 원본은 삭제되지 않는다.

자동 검증: `test_controller_move_split_delete_and_shortcuts_use_real_history`와
`tests/timeline/test_editing.py`의 분할·삭제·리플·명령 왕복 검증.

### V-MVP-05: 저장, 다른 이름으로 저장과 다시 열기

연결 기능: `F-PROJECT-02`~`06`, `F-MEDIA-06`.

1. 수정된 프로젝트에서 `저장`을 실행한다.
2. 다시 편집하고 새 프로젝트 또는 프로젝트 열기를 실행한다.
3. 저장, 저장하지 않고 계속, 취소를 각각 확인한다.
4. `다른 이름으로 저장`을 실행한다.
5. 저장한 프로젝트 파일을 파일 대화상자에서 다시 연다.

합격 기준:

- 첫 저장은 실제 경로를 선택하고 원자적 교체가 성공한 뒤 `*`를 없앤다.
- 저장 확인은 프로젝트명과 실제 결과를 말하는 동사형 버튼을 사용한다.
- 취소는 프로젝트, 선택과 편집 내용을 그대로 유지한다.
- 누락 원본이 있어도 프로젝트 구조와 편집점을 보존하고 누락 상태를 표시한다.
- 저장·열기 취소나 실패는 현재 프로젝트, 경로, 이름과 dirty를 유지한다.

자동 검증: `test_real_save_marks_current_result_clean_without_serialising_history`,
`test_controller_save_as_and_open_round_trip_clear_new_session_history`,
`test_unsaved_new_project_confirmation_handles_all_three_branches`.

### V-MVP-06: 보관함 항목 제거

연결 기능: `F-MEDIA-04`, `F-TIMELINE-03`.

1. 타임라인에서 사용하지 않는 항목을 보관함에서 제거한다.
2. 사용 중인 기준 미디어 제거를 시도한다.
3. 관련 클립 수와 클립을 먼저 제거하라는 안내를 확인한다.

합격 기준:

- 미사용 항목은 즉시 보관함에서만 제거된다.
- 사용 중 항목은 관련 클립 수를 알리고 제거를 거부한다.
- 성공과 거부 모두 컴퓨터의 원본 파일 내용과 메타데이터를 바꾸지 않는다.
- 성공한 제거는 W-01 명령 이력에 기록된다.

자동 검증: `test_unused_real_item_removal_keeps_source_file_and_removes_core_reference`,
`test_used_real_item_removal_is_refused_with_impact_count`.

### V-MVP-07: 미리 보기와 탐색

연결 기능: `F-PREVIEW-01`~`05`, `F-EXPERIENCE-01`.

1. 시각 클립이 없는 상태에서 재생 명령을 확인한다.
2. 샘플 프로젝트에서 재생, 일시 정지, 슬라이더 탐색과 다시 재생을 수행한다.
3. 끝까지 재생한 뒤 다시 재생한다.
4. 미리 보기 음소거를 켜고 끈다.
5. 이전·다음 프레임 버튼을 눌러 원본 프레임률 또는 time base 경계 이동을 확인한다.
6. 영상 원본음 음량·음소거와 음악 시작·사용 구간·음량을 바꾸고 다시 재생한다.

합격 기준:

- 빈 상태는 미디어 추가라는 다음 행동을 제시한다.
- 시간, 미리 보기 장면과 타임라인 재생 헤드가 같은 위치를 표시한다.
- 끝에서 다시 재생하면 0초로 돌아가며 프로젝트 설정 음량은 바뀌지 않는다.
- source in과 재생 속도가 반영된 실제 영상·사진 프레임을 표시한다.
- 같은 source in·속도와 프로젝트 위치가 영상 원본음과 음악에도 반영된다.
- 미리 보기 음소거는 저장된 클립 음량을 바꾸지 않고 탐색·편집은 오래된 오디오를 취소한다.
- 23.976/29.97/59.94fps도 절대 프레임 인덱스에서 계산해 누적 오차가 없다.
- 빠른 연속 탐색과 프로젝트 교체에서는 오래된 프레임이 표시되지 않는다.
- 누락·손상·미지원 미디어는 원인과 다음 행동을 표시하고 편집 상태를 바꾸지 않는다.

자동 검증: `tests/preview/`, `tests/audio/`, `test_preview_playback_integration.py`,
`test_audio_playback_integration.py`와 `test_empty_preview_explains_next_action`.

### V-MVP-08: 출력 설정, 완료와 취소

연결 기능: `F-EXPORT-01`~`04`, `06`, `F-VISUAL-04`.

1. 시각 클립이 없는 상태에서 `동영상 저장`의 비활성 이유를 확인한다.
2. 샘플 프로젝트에서 출력 설정을 열고 원본 유지, 720p, 1080p를 비교한다.
3. 원본 유지의 기준, 혼합 비율 처리, 1.0 프레임률·품질, 형식과 길이 요약을 읽는다.
4. 출력을 시작해 완료하거나 진행 중 취소한다.

합격 기준:

- 원본 유지는 1920×1080 기준 미디어를, 720p와 1080p는 고정 16:9 크기를 표시한다.
- 진행률, 현재 단계, 취소와 결과 행동을 편집 화면을 잃지 않고 확인할 수 있다.
- 취소는 다음 주기에 완료되고 불완전 파일이 없다는 목업 정책을 말한다.
- 완료 행동도 실제 MP4가 만들어지지 않았음을 명시한다.

자동 검증: `test_export_dialog_explains_original_and_fixed_presets`,
`test_export_progress_panel_exposes_complete_and_failure_states`,
`test_export_can_complete_cancel_and_fail_without_creating_a_file`.

## 1.0 대표 흐름

### V-10-01: 행동 이력, 복제와 다중 선택

연결 기능: `F-TIMELINE-08`~`14`, `F-EXPERIENCE-05`.

1. 클립을 복제하고 이동한다.
2. `Ctrl` 선택으로 같은 트랙의 두 클립을 선택해 함께 삭제한다.
3. 실행 취소, 다시 실행, 다시 실행 취소 뒤 다른 편집을 수행한다.
4. 타임라인·스토리보드 보기와 확대 수준을 바꾼다.

합격 기준:

- 다중 선택은 선택 수와 공통 명령만 표시하며 서로 다른 트랙을 암묵적으로 묶지 않는다.
- 실행 취소와 다시 실행 메뉴는 다음 행동의 이름을 표시한다.
- 실행 취소 뒤 새 편집을 하면 이전 다시 실행 후보가 사라진다.
- 보기와 확대 변경은 프로젝트 편집 내용이나 행동 이력을 바꾸지 않는다.

자동 검증: `test_multi_selection_deletes_compatible_clips_as_one_command`,
`test_ctrl_style_multi_selection_is_reflected_in_inspector`,
`test_split_undo_redo_and_history_branch_preserve_timeline_length`.

### V-10-02: 내레이션과 오디오 속성

연결 기능: `F-AUDIO-05`~`11`, `F-MEDIA-08`, `09`.

1. `내레이션 녹음`을 열고 가짜 입력 수준, 시작, 일시 정지와 완료를 확인한다.
2. 완료한 5초 목업 녹음을 내레이션 트랙에 추가한다.
3. 시작, 사용 구간, 음량, 음소거, 페이드와 더킹을 적용한다.
4. 음악 트랙의 장식 파형과 백그라운드 작업 상태를 확인한다.
5. 영상 미디어에서 프록시 사용·해제를 바꾸고 고정된 목업 준비 상태를 확인한다.

합격 기준:

- 실제 마이크를 사용하지 않는다는 사실이 녹음 전후에 분명하다.
- 음악과 내레이션은 서로 다른 고정 트랙과 속성 문맥을 사용한다.
- 페이드 합이 클립보다 길면 적용하지 않고 이유를 표시한다.
- 목업 파형을 실제 분석 결과로 표현하지 않는다.
- 프록시 상태는 실제 파일 생성 없이 보관함과 상태 표시줄의 준비 개수에 함께 반영된다.

자동 검증: `test_text_transition_and_narration_mock_commands_update_fixed_tracks`,
`test_proxy_toggle_records_a_mock_media_edit_without_creating_files`.
녹음 타이머와 속성 입력 제한은 수동 검증한다.

### V-10-03: 제목, 캡션과 크레딧

연결 기능: `F-TEXT-01`~`06`.

1. 시작에 제목, 재생 헤드에 캡션, 끝에 크레딧을 추가한다.
2. 내용, 글꼴, 크기, 굵기, 색상, 정렬, 위치, 표시 시간과 애니메이션을 바꾼다.
3. 빈 내용을 적용한다.

합격 기준:

- 생성 위치 기본값이 텍스트 종류와 일치한다.
- 속성은 한 패널에서 바뀌고 타임라인과 현재 위치의 미리 보기에 반영된다.
- 빈 내용은 허용하되 미리 보기에는 `텍스트를 입력하세요`를 표시한다.
- 안전 영역과 가독성 기본값을 유지한다.

자동 검증: `test_text_transition_and_narration_mock_commands_update_fixed_tracks`.
글꼴·색상·정렬 조합은 수동 검증한다.

### V-10-04: 화면 배치, 회전, 효과와 전환

연결 기능: `F-VISUAL-01`~`08`.

1. 시각 클립을 선택해 맞춤·채움, 90도 회전과 기본 효과를 바꾼다.
2. 다음 클립이 있는 앞쪽 클립에서 전환 속성을 연다.
3. 없음, 페이드 또는 기본 전환과 길이를 적용한다.

합격 기준:

- 맞춤과 채움은 여백 또는 잘림의 의미를 미리 보기에서 구분한다.
- 회전은 입력 메타데이터와 별개인 편집값으로 90도 단위 기록된다.
- 전환은 인접 경계에만 놓이며 양쪽 클립이 허용하는 길이를 넘지 않는다.
- 제한된 프리셋임을 보여 주고 고급 합성 도구처럼 확장하지 않는다.

자동 검증: 속성 명령과 전환 상태는 컨트롤러 테스트가 확인한다. 시각적 구분은 수동 검증한다.

## 오류와 회복 흐름

### V-ERROR-01: 누락 미디어와 다시 연결

연결 기능: `F-PROJECT-04`, `F-MEDIA-06`, `07`, `F-EXPERIENCE-02`.

1. `누락 미디어 추가`를 실행한다.
2. 보관함 배지, 속성 상태와 누락 미디어 화면을 확인한다.
3. 누락 상태로 열기와 1.0 다시 연결을 각각 수행한다.

합격 기준:

- 마지막 경로, 영향을 받는 클립 수와 가능한 다음 행동을 함께 표시한다.
- 누락 상태에서도 프로젝트 구조와 편집점은 보존한다.
- 잘못된 원본을 조용히 연결하지 않는 실제 검증 경계를 명세에 남긴다.

자동 검증: `test_missing_media_and_partial_failure_remain_visible_and_recoverable`.

### V-ERROR-02: 부분 가져오기 실패

연결 기능: `F-MEDIA-03`, `09`, `F-EXPERIENCE-02`.

1. 정상 영상·사진·오디오, 손상 파일과 미지원 파일을 한 번에 선택한다.
2. 성공 항목과 파일별 실패 이유, 썸네일 결과를 확인한다.
3. 성공 파일 하나를 다시 선택해 중복 건너뜀을 확인한다.

합격 기준:

- 한 파일의 실패가 정상 항목을 제거하지 않는다.
- 오류 결과는 파일명과 실패 이유를 유지하고 유효하지 않은 프로젝트 항목을 만들지 않는다.
- 썸네일 실패는 미디어 항목을 유지하고 기본 아이콘과 경고를 표시한다.
- 보관함 전체가 비었다는 상태와 필터 결과가 없다는 상태를 구분한다.

자동 검증: `test_mixed_batch_keeps_video_photo_audio_successes_and_file_failures`,
`test_main_window_import_action_uses_selected_files_and_shows_failures`.

### V-ERROR-03: 출력 실패와 재시도

연결 기능: `F-EXPORT-05`, `07`, `F-EXPERIENCE-02`.

1. 쓰기 권한, FFmpeg 없음 또는 디스크 공간 부족 중 다음 실패를 예약한다.
2. 출력 설정을 열고 시작한다.
3. 실패 이유, 설정으로 돌아가기와 다시 시도 경로를 확인한다.

합격 기준:

- 실패는 진행 패널에 원인과 다음 행동을 함께 보여 준다.
- 설정으로 돌아가도 출력 경로와 프리셋을 유지한다.
- 편집 프로젝트와 행동 이력은 실패 때문에 바뀌지 않는다.

자동 검증: `test_export_can_complete_cancel_and_fail_without_creating_a_file`,
`test_export_progress_panel_exposes_complete_and_failure_states`.

### V-ERROR-04: 자동 저장 복구와 실행 환경

연결 기능: `F-PROJECT-07`~`09`, `F-RUNTIME-01`~`06`, `F-EXPERIENCE-02`.

1. 자동 저장 복구 화면에서 두 저장 시점과 변경 요약을 비교한다.
2. 자동 저장본, 정상 저장본과 나중에 결정 경로를 확인한다.
3. 더 새 버전 프로젝트 오류에서 원본을 바꾸지 않은 상태와 필요한 앱 버전을 확인한다.
4. 런타임 화면에서 환경 상태, 로그, 복구, 옵션, 실행과 유지관리 진입을 확인한다.

합격 기준:

- 복구본은 정상 저장본을 즉시 덮어쓰지 않는다.
- 런타임 화면은 실제 편집기와 별도 책임을 유지한다.
- 목업 버튼은 설치, 실행 또는 파일 변경을 실제 수행했다고 주장하지 않는다.

자동 검증: `test_mock_state_actions_open_named_supporting_screens`.

## 레이아웃·키보드·접근성

### V-UX-01: 창 크기

| 크기 | 합격 기준 | 기준 화면 |
| --- | --- | --- |
| 1024×640 | 보관함과 핵심 편집 영역 유지, 속성 패널 자동 접힘, 메뉴로 모든 명령 접근 | [화면](screenshots/editor-sample-1024x640.png) |
| 1280×720 | 보관함·미리 보기·속성·네 트랙 동시 표시, 핵심 조작 잘림 없음 | [화면](screenshots/editor-sample-1280x720.png) |
| 1920×1080 | 패널 폭은 과도하게 늘지 않고 중앙 미리 보기와 타임라인이 남는 공간 사용 | [화면](screenshots/editor-sample-1920x1080.png) |

빈 상태와 출력 설정 기준은 [빈 편집기](screenshots/editor-empty-1280x720.png),
[출력 설정](screenshots/export-settings-original.png)에서 확인한다.

자동 검증: `test_required_screen_regions_fit_at_supported_window_sizes`.

### V-UX-02: 포커스와 키보드

연결 기능: `F-EXPERIENCE-03`~`05`.

1. `Tab`과 `Shift+Tab`으로 도구 모음, 보관함, 미리 보기, 속성과 타임라인을 이동한다.
2. `Ctrl+N/O/S/Shift+S/I/E`, `Space`, `Delete`, `Ctrl+B/D/Z/Y`를 실행한다.
3. 보관함과 타임라인에서 각각 `Delete`를 확인한다.
4. 대화상자에서 `Esc`를 누른다.

합격 기준:

- 포커스는 시각적 배경과 독립적인 테두리로 보인다.
- 비활성 명령은 실행되지 않고 메뉴·도구 설명에서 필요한 조건을 알 수 있다.
- `Delete` 대상은 현재 포커스 영역과 선택 표시로 예측할 수 있다.
- `Esc`는 파괴적 행동을 승인하지 않고 대화상자를 취소한다.

단축키 등록과 상세 요소 존재는 `test_detailed_mvp_and_one_zero_controls_exist_in_context` 및
Qt 동작으로 자동 확인하고, 전체 Tab 순서와 Windows 고대비 테마는 수동 검증한다.

### V-UX-03: 시작 안내와 한국어 기준 문구

연결 기능: `F-EXPERIENCE-06`, `07`.

1. 도움말에서 시작 안내를 열고 첫 단계부터 마지막 단계까지 차례로 읽는다.
2. 건너뛰기는 현재 프로젝트를 유지하고 예제 열기는 샘플 프로젝트로 전환하는지 확인한다.
3. 1024×640과 1280×720에서 한국어 메뉴, 오류, 긴 상태 문구의 잘림을 확인한다.

합격 기준:

- 안내 없이도 편집기를 쓸 수 있고 시작 안내는 언제든 다시 열고 건너뛸 수 있다.
- 안내의 가져오기·편집·출력 용어가 실제 메뉴 및 버튼 이름과 일치한다.
- 1.0 기준 언어인 한국어에서 핵심 행동과 오류의 다음 단계가 생략되거나 잘리지 않는다.
- 추가 언어는 문자열을 UI 구조와 분리한 뒤 같은 레이아웃·용어 검증을 별도로 통과한다.

자동 검증: `test_mock_state_actions_open_named_supporting_screens`. 전체 문구와 번역 확장은
수동 검증한다.

## 5단계 완료 확인

- [x] 고정 샘플 데이터와 가짜 상태 변화로 실행 가능한 PySide6 목업을 만들었다.
- [x] 화면과 목업 컨트롤러를 분리하고 사용자 의도를 단일 경계로 전달한다.
- [x] MVP의 가져오기, 배치, 편집, 미리 보기, 저장과 출력 흐름을 연결했다.
- [x] 1.0의 행동 이력, 스토리보드, 내레이션, 텍스트, 전환과 복구 화면을 연결했다.
- [x] 빈 상태, 누락, 부분 실패, 출력 취소·완료·실패 상태를 재현할 수 있다.
- [x] 1024×640, 1280×720과 1920×1080 기준 화면을 캡처했다.
- [x] 대표 상태와 UI 계약을 자동화 테스트로 검증했다.
- [x] 실제 파일·미디어 처리와 목업 결과의 경계를 화면과 문서에 명시했다.

검토 결과와 실제 서비스 연결 순서는 [MOCK-0003](mock-0003-review-log.md)에서 확정한다.
