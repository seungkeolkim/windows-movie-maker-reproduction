# Movie Maker Reproduction 0.1.0

첫 Windows 설치 미리보기 릴리스입니다.

- 터미널 없이 uv, 고정 Python, 잠금 환경과 FFmpeg 상태를 점검합니다.
- 사용자가 승인한 경우에만 잠긴 런타임 환경을 구성하거나 복구합니다.
- `.mmrproj` 파일 연결과 `--project` 시작을 지원합니다.
- 시작 메뉴, 선택적 바탕 화면 바로가기와 선택적 사용자 PATH 등록을 지원합니다.
- 업데이트 실패 시 기존 설치를 복원하고, 제거 시 프로젝트와 원본 미디어를 보존합니다.

이 미리보기는 환경 구성을 위한 검증된 uv 0.12.1을 포함하지만 Python, `.venv`, PySide6 또는
FFmpeg 바이너리는 포함하지 않습니다. 따라서 사용자가 .NET SDK나 C/C++ 빌드 도구 또는 uv를
별도로 설치할 필요가 없습니다. 릴리스 폴더의 `SIGNING-STATUS.txt`가 `unsigned`이면 코드 서명
인증서 없이 만들어진 시험 산출물입니다.
